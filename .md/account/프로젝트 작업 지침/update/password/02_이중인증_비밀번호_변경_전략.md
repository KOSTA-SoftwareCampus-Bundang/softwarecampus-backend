# 비밀번호 변경 - 이중 인증 전략 설계

## 📋 개요

**목적:** 로그인 상태에서 비밀번호를 변경할 때, **현재 비밀번호 검증 + 이메일 인증**의 이중 보안 적용  
**방식:** 3단계 보안 플로우 (현재 비번 확인 → 이메일 인증 → 비번 변경)  
**보안 수준:** 높음 (세션 탈취 공격 방어)  
**대상:** 로그인한 사용자의 마이페이지 비밀번호 변경

---

## 🎯 변경 배경

### 기존 방식의 문제점
기존 구현은 로그인 상태에서 **새 비밀번호만 입력**하면 즉시 변경되었습니다.

```
[기존 플로우]
1. 로그인 (JWT 토큰 발급)
2. 마이페이지 → "비밀번호 변경" 버튼 클릭
3. 새 비밀번호 입력
4. PUT /api/mypage/password → 즉시 변경
```

**보안 취약점:**
- 세션 하이재킹 시 공격자가 비밀번호 임의 변경 가능
- 공용 PC에서 로그인 유지 시 타인이 비밀번호 변경 가능
- JWT 토큰 탈취 시 계정 완전 탈취 가능

### 새로운 이중 인증 방식
**현재 비밀번호 확인** + **이메일 인증 코드**를 모두 통과해야 변경 가능

```
[새로운 플로우]
1. 로그인 (JWT 토큰 발급)
2. 마이페이지 → "비밀번호 변경" 버튼 클릭
3. [1단계] 현재 비밀번호 입력 → 검증 API 호출
4. [2단계] 검증 성공 → 이메일 인증 코드 자동 발송
5. [3단계] 인증 코드 + 새 비밀번호 입력 → 최종 변경
```

---

## 🏗️ 아키텍처 설계

### 1. API 구조

| 단계 | API | 목적 | 신규/기존 |
|------|-----|------|-----------|
| 1단계 | `POST /api/auth/verify-password` | 현재 비밀번호 검증 | 🆕 신규 |
| 2단계 | `POST /api/auth/email/send-change-code` | 이메일 인증 코드 발송 | 🆕 신규 |
| 3단계 | `PUT /api/mypage/password` | 최종 비밀번호 변경 | 📝 수정 |

### 2. 레이어 구조

```
src/main/java/com/softwarecampus/backend/
├── controller/user/
│   ├── AuthController.java          ← verifyPassword() 추가
│   ├── EmailVerificationController.java ← sendPasswordChangeCode() 추가
│   └── MyPageController.java        ← changePassword() 수정
├── service/user/
│   ├── login/
│   │   ├── LoginService.java        ← verifyPassword() 선언
│   │   └── LoginServiceImpl.java    ← verifyPassword() 구현
│   ├── email/
│   │   └── EmailVerificationServiceImpl.java ← PASSWORD_CHANGE 지원
│   └── profile/
│       └── ProfileServiceImpl.java  ← changePassword() 수정
├── dto/user/
│   ├── VerifyPasswordRequest.java   ← 🆕 신규
│   └── ChangePasswordRequest.java   ← 📝 수정 (verificationCode 추가)
└── domain/common/
    └── VerificationType.java        ← PASSWORD_CHANGE 추가
```

---

## 📝 API 명세

### 1단계: 현재 비밀번호 검증

**Endpoint:** `POST /api/auth/verify-password`  
**인증:** JWT 토큰 필수

**Request:**
```json
{
  "currentPassword": "현재비밀번호123!"
}
```

**Response (200 OK):**
```json
{
  "verified": true,
  "message": "비밀번호가 확인되었습니다."
}
```

**Error Cases:**
- `400 Bad Request`: 비밀번호 불일치
- `401 Unauthorized`: JWT 토큰 없음/만료
- `404 Not Found`: 계정 없음

---

### 2단계: 비밀번호 변경용 인증 코드 발송

**Endpoint:** `POST /api/auth/email/send-change-code`  
**인증:** JWT 토큰 필수

**Request:**
```json
{
  "email": "user@example.com"
}
```

**Response (200 OK):**
```json
{
  "success": true,
  "message": "인증 코드가 이메일로 발송되었습니다.",
  "expiresAt": "2025-11-30T12:03:00"
}
```

**⚠️ 보안:** 이 API는 1단계(현재 비밀번호 검증)를 통과한 후에만 호출되어야 합니다.  
프론트엔드에서 순서를 제어합니다.

---

### 3단계: 최종 비밀번호 변경

**Endpoint:** `PUT /api/mypage/password`  
**인증:** JWT 토큰 필수

**Request:**
```json
{
  "verificationCode": "123456",
  "newPassword": "새비밀번호456@"
}
```

**⚠️ 참고:** 
- 1단계에서 이미 현재 비밀번호를 확인했으므로, 이 요청에는 `currentPassword`가 포함되지 않습니다.
- 이메일 인증 코드 검증 + 새 비밀번호로 변경을 한 번에 처리합니다.

**Response (200 OK):**
```json
{
  "message": "비밀번호가 성공적으로 변경되었습니다."
}
```

**Error Cases:**
- `400 Bad Request`: 인증 코드 불일치/만료, 비밀번호 규칙 위반
- `404 Not Found`: 인증 요청 없음 (2단계 미수행)
- `429 Too Many Requests`: 인증 시도 횟수 초과

---

## 🛠️ 구현 상세

### 1. VerificationType Enum 수정

```java
// src/main/java/.../domain/common/VerificationType.java
public enum VerificationType {
    SIGNUP("회원가입"),
    PASSWORD_RESET("비밀번호 재설정"),      // 비로그인 상태 (비번 찾기)
    PASSWORD_CHANGE("비밀번호 변경");       // 🆕 로그인 상태 (마이페이지)
    
    // ...
}
```

---

### 2. VerifyPasswordRequest DTO (신규)

```java
package com.softwarecampus.backend.dto.user;

import jakarta.validation.constraints.NotBlank;
import lombok.AllArgsConstructor;
import lombok.Getter;
import lombok.NoArgsConstructor;

/**
 * 현재 비밀번호 검증 요청 DTO
 */
@Getter
@NoArgsConstructor
@AllArgsConstructor
public class VerifyPasswordRequest {
    
    @NotBlank(message = "현재 비밀번호를 입력해주세요")
    private String currentPassword;
}
```

---

### 3. ChangePasswordRequest DTO (수정)

```java
package com.softwarecampus.backend.dto.user;

import jakarta.validation.constraints.NotBlank;
import jakarta.validation.constraints.Pattern;
import jakarta.validation.constraints.Size;
import lombok.AllArgsConstructor;
import lombok.Getter;
import lombok.NoArgsConstructor;

/**
 * 비밀번호 변경 요청 DTO (이메일 인증 코드 기반)
 */
@Getter
@NoArgsConstructor
@AllArgsConstructor
public class ChangePasswordRequest {
    
    @NotBlank(message = "인증 코드를 입력해주세요")
    @Size(min = 6, max = 6, message = "인증 코드는 6자리여야 합니다")
    @Pattern(regexp = "^[0-9]{6}$", message = "인증 코드는 6자리 숫자여야 합니다")
    private String verificationCode;
    
    @NotBlank(message = "새 비밀번호를 입력해주세요")
    @Size(min = 8, max = 50, message = "비밀번호는 8~50자여야 합니다")
    @Pattern(
        regexp = "^(?=.*[A-Za-z])(?=.*\\d)(?=.*[!@#$%^&*()_+\\-=\\[\\]{}|;:'\",.<>?/]).{8,50}$",
        message = "비밀번호는 영문, 숫자, 특수문자를 포함해야 합니다"
    )
    private String newPassword;
}
```

---

### 4. LoginService - 비밀번호 검증 메서드

```java
// LoginService.java (인터페이스)
/**
 * 현재 비밀번호 검증 (마이페이지용)
 * 
 * @param email 로그인한 사용자 이메일
 * @param currentPassword 입력한 현재 비밀번호
 * @return 검증 성공 여부
 * @throws AccountNotFoundException 계정이 존재하지 않는 경우
 * @throws InvalidPasswordException 비밀번호가 일치하지 않는 경우
 */
boolean verifyPassword(String email, String currentPassword);
```

```java
// LoginServiceImpl.java (구현)
@Override
public boolean verifyPassword(String email, String currentPassword) {
    Account account = accountRepository.findByEmail(email)
        .orElseThrow(() -> new AccountNotFoundException("계정을 찾을 수 없습니다."));
    
    if (!passwordEncoder.matches(currentPassword, account.getPassword())) {
        log.warn("비밀번호 검증 실패: email={}", EmailUtils.maskEmail(email));
        throw new InvalidPasswordException("현재 비밀번호가 일치하지 않습니다.");
    }
    
    log.info("비밀번호 검증 성공: email={}", EmailUtils.maskEmail(email));
    return true;
}
```

---

### 5. AuthController - 비밀번호 검증 엔드포인트

```java
// AuthController.java
@PostMapping("/verify-password")
public ResponseEntity<?> verifyPassword(
        @AuthenticationPrincipal UserDetails userDetails,
        @Valid @RequestBody VerifyPasswordRequest request) {
    
    String email = userDetails.getUsername();
    log.info("현재 비밀번호 검증 요청: email={}", EmailUtils.maskEmail(email));
    
    loginService.verifyPassword(email, request.getCurrentPassword());
    
    return ResponseEntity.ok(Map.of(
        "verified", true,
        "message", "비밀번호가 확인되었습니다."
    ));
}
```

---

### 6. EmailVerificationController - 인증 코드 발송

```java
// EmailVerificationController.java
@PostMapping("/send-change-code")
public ResponseEntity<EmailVerificationResponse> sendPasswordChangeCode(
        @AuthenticationPrincipal UserDetails userDetails,
        @Valid @RequestBody EmailVerificationRequest request) {
    
    String email = userDetails.getUsername();
    log.info("비밀번호 변경 인증 코드 발송 요청 - type: {}", VerificationType.PASSWORD_CHANGE);
    
    // JWT 이메일과 요청 이메일 일치 확인
    if (!email.equals(request.getEmail())) {
        return ResponseEntity.status(HttpStatus.FORBIDDEN).build();
    }
    
    // 강제로 PASSWORD_CHANGE 타입 설정
    request.setType(VerificationType.PASSWORD_CHANGE);
    
    EmailVerificationResponse response = verificationService.sendVerificationCode(request);
    return ResponseEntity.ok(response);
}
```

---

### 7. ProfileServiceImpl - 비밀번호 변경 (수정)

```java
// ProfileServiceImpl.java
@Override
@Transactional
public void changePassword(String email, ChangePasswordRequest request) {
    log.info("비밀번호 변경 시도: email={}", EmailUtils.maskEmail(email));
    
    // 1. 이메일 인증 코드 검증 (PASSWORD_CHANGE 타입)
    EmailVerificationCodeRequest codeRequest = 
        new EmailVerificationCodeRequest(email, request.getVerificationCode());
    
    // verifyCode 메서드 재사용 - PASSWORD_CHANGE 타입으로 조회
    emailVerificationService.verifyChangeCode(codeRequest);
    
    // 2. Account 조회
    Account account = accountRepository.findByEmail(email)
        .orElseThrow(() -> new AccountNotFoundException("계정을 찾을 수 없습니다."));
    
    // 3. 새 비밀번호 암호화 및 저장
    String encodedPassword = passwordEncoder.encode(request.getNewPassword());
    account.setPassword(encodedPassword);
    
    log.info("비밀번호 변경 완료: email={}", EmailUtils.maskEmail(email));
}
```

---

## 🔐 보안 정책

### 1단계 보안 (현재 비밀번호 검증)
- **목적:** 세션 탈취 공격자가 비밀번호를 모르면 변경 불가
- **방어:** JWT 토큰만으로는 비밀번호 변경 불가능

### 2단계 보안 (이메일 인증)
- **목적:** 이메일 소유권 확인
- **방어:** 이메일 계정도 탈취해야 비밀번호 변경 가능
- **제한:** 6자리 코드, 3분 유효, 5회 시도 제한

### 이중 인증의 장점
```
공격 시나리오             | 기존 방식 | 이중 인증
--------------------------|-----------|----------
JWT 토큰 탈취             | ❌ 탈취   | ✅ 방어 (현재 비번 모름)
세션 하이재킹             | ❌ 탈취   | ✅ 방어 (현재 비번 모름)
공용 PC 방치              | ❌ 탈취   | ✅ 방어 (현재 비번 모름)
JWT + 현재비번 유출       | ❌ 탈취   | ✅ 방어 (이메일 인증 필요)
JWT + 현재비번 + 이메일   | ❌ 탈취   | ❌ 탈취 (모두 유출 시)
```

---

## ✅ 구현 체크리스트

### Backend
- [ ] `VerificationType.PASSWORD_CHANGE` 추가
- [ ] `VerifyPasswordRequest` DTO 생성
- [ ] `ChangePasswordRequest` DTO 수정 (verificationCode 추가)
- [ ] `InvalidPasswordException` 예외 클래스 생성
- [ ] `LoginService.verifyPassword()` 메서드 추가
- [ ] `AuthController.verifyPassword()` 엔드포인트 추가
- [ ] `EmailVerificationController.sendPasswordChangeCode()` 추가
- [ ] `EmailVerificationService.verifyChangeCode()` 메서드 추가
- [ ] `ProfileServiceImpl.changePassword()` 로직 수정
- [ ] `GlobalExceptionHandler`에 InvalidPasswordException 핸들러 추가

### Frontend (참고용)
- [ ] 비밀번호 변경 플로우 UI 재설계
- [ ] 1단계: 현재 비밀번호 입력 모달
- [ ] 2단계: 이메일 인증 코드 입력 UI
- [ ] 3단계: 새 비밀번호 입력 UI
- [ ] 각 단계별 API 호출 로직 구현

---

## 📅 예상 일정

| 작업 | 예상 소요 시간 |
|------|---------------|
| Backend API 구현 | 2-3시간 |
| 단위 테스트 작성 | 1-2시간 |
| Frontend UI 구현 | 3-4시간 |
| 통합 테스트 | 1시간 |

**총 예상:** 7-10시간

---

**작성일:** 2025년 11월 30일  
**작성자:** GitHub Copilot  
**문서 버전:** 1.0  
**상태:** 설계 완료, 구현 대기
